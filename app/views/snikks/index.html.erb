<h1>Listing snikks</h1>

<table>
  <tr>
    <th>Content</th>
    <th>Url</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @snikks.each do |snikk| %>
  <tr>
    <td><%= snikk.content %></td>
    <td><%= snikk.url %></td>
    <td><%= snikk.user_id %></td>
    <td><%= link_to 'Show', snikk %></td>
    <td><%= link_to 'Edit', edit_snikk_path(snikk) %></td>
    <td><%= link_to 'Destroy', snikk, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Snikk', new_snikk_path %>
